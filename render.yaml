services:
  - type: web
    name: leadflow-ai-backend
    runtime: node
    region: oregon
    plan: free
    buildCommand: 'cd backend && npm install'
    startCommand: 'cd backend && node server.js'
    envVars:
      - key: OPENAI_API_KEY
        sync: false
